import os
import subprocess
import hashlib




# cwd = os.getcwd()
# entries = os.listdir()


# for entry in entries:
    # pass


command = "$EDITOR"
process = subprocess.Popen(command, shell=True, stdout=subprocess.PIPE)
process.wait()

# 


# object containing all files and hashes
# write out object to file
# let user edit file
# upon user exiting process the file and make changes



hashlib.sha224("Nobody inspects the spammish repetitiona").hexdigest()

